const config = {
    gameWidth: 800,
    gameHeight: 520,
    tileSize: 40,
    normal: {
        tileDestroyDelay: 480,
        playerSpeed: 120
    },
    easy: {
        tileDestroyDelay: 800,
        playerSpeed: 80
    }

};
export default config;
